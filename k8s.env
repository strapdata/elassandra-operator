# Useful functions for dev

function op() {
 kubectl logs -f $(kubectl get pod -l app=elassandra-operator -n default --no-headers | awk '{ print $1 }')
}

function oplog() {
 kubectl logs  $(kubectl get pod -l app=elassandra-operator -n default  --no-headers | awk '{ print $1 }') | less
}

function opkill() {
 kubectl delete pod -l app=elassandra-operator -n default
}
