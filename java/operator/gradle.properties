# the last part of the image name
dockerImageSuffix=elassandra-operator

# TODO: make this an option at runtime, not build time (need to configure the entrypoint generated by jib)
# enable debuging
operatorEnableDebug=false
# suspend the operator startup to allow debuger attachement
operatorDebugSuspend=false
operatorDebugPort=5005